export class Feat {
  /**
   * The name of the Feat.
   */
  name: string;
  /**
   * Any prerequisites (e.g. "tiefling", "warlock level 5")
   */
  prerequisite: string;
  /**
   * The text of the feat (in Markdown).
   */
  text: string;
}
